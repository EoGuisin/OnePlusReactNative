import Local from "../Local";
import SubLocal from "../SubLocal";
import SubLocal02 from "../SubLocal02";

export default interface SubLocal03 {
    local: Local;
    subLocal: SubLocal;
    subLocal02: SubLocal02;
    id: number;
    descricao: string;
}